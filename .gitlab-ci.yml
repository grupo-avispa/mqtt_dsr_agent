stages:
  - build

image: grupoavispa/cortex:development-latest

before_script:
  - DEBIAN_FRONTEND=noninteractive apt-get update

build:
  stage: build
  script:
    - apt-get install -y cmake g++ make git openssl libssl-dev nlohmann-json3-dev libeigen3
    - mkdir build && cd build
    - cmake ..
    - make